"""Single source of truth for the application version (Semantic Versioning).

Bump this file according to https://semver.org/ and add a matching entry to
CHANGELOG.md before tagging a release (see docs/OPERATOR_AND_MAINTAINER_GUIDE.md
for the release process). Keep this as the only place the version string is
hardcoded -- everything else (README badge, API responses, git tag) should be
derived from or kept in sync with this value.
"""

__version__ = "1.1.0"
